# === TEST ENVIRONMENT CONFIGURATION ===

# JWT
SECRET_KEY=test_secret
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7
RATE_LIMIT_PER_MINUTE=100

# Database (локальна PostgreSQL)
POSTGRES_DB=test_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=567234
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
DATABASE_URL=postgresql+psycopg2://postgres:567234@db:5432/test_db

# Redis (опціонально, якщо він використовується у тестах)
REDIS_URL=redis://localhost:6379/0

# Email (Mailhog — необов’язковий для тестів)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=

# Cloudinary (тестові ключі або пусті)
CLOUDINARY_CLOUD_NAME=dummy
CLOUDINARY_API_KEY=dummy
CLOUDINARY_API_SECRET=dummy

# Rate limit
RATE_LIMIT_PER_MINUTE=10
